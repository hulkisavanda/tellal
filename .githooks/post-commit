#!/bin/sh
# Her commit sonrası mevcut dalı origin'e iter. Kurulum: npm install veya npm run hooks
branch=$(git symbolic-ref --short HEAD 2>/dev/null) || exit 0
[ -z "$branch" ] && exit 0
git push origin "$branch"
